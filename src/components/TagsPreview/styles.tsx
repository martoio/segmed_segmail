import styled from 'styled-components';
import {Badge} from 'react-bootstrap';

export const TagsWrapper = styled.div``;

export const ActiveTagsWrapper = styled.div``;

export const InactiveTagsWrapper = styled.div`
    .badge {
        cursor: pointer;
    }
`;